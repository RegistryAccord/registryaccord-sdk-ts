# Documentation Update Prompt

**Context**: Update documentation to match code changes.

**Scope**:
- **README.md**: Update "Installation" or "Quick Start" if public API changed.
- **TSDoc**: Ensure every exported class/method has `@param` and `@returns` tags.
- **Examples**: Verify scripts in `examples/` still run correctly.
